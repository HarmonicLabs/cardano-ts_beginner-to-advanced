{
  "type": "module",
  "scripts": {
    "build": "rm -rf ./dist && tsc --project ./tsconfig.json && tsc-alias -p ./tsconfig.json",
    "build:light": "tsc --project ./tsconfig.json && tsc-alias -p ./tsconfig.json",
    "buidl": "npm run build",
    "fund": "npm run build && node dist/fund.js",
    "spend": "npm run build && node dist/spend.js"
  },
  "dependencies": {
    "@harmoniclabs/blockfrost-pluts": "^0.1.14",
    "@harmoniclabs/plu-ts": "^0.8.0-dev9",
    "@harmoniclabs/uint8array-utils": "^1.0.0",
    "bip39": "^3.1.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.11"
  }
}
