{
  "type": "module",
  "scripts": {
    "build": "rm -rf ./dist && tsc --project ./tsconfig.json && tsc-alias -p ./tsconfig.json",
    "build:light": "tsc --project ./tsconfig.json && tsc-alias -p ./tsconfig.json",
    "buidl": "npm run build",
    "start": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "@harmoniclabs/blockfrost-pluts": "^0.1.13",
    "@harmoniclabs/plu-ts": "^0.8.0-dev5",
    "@harmoniclabs/uint8array-utils": "^1.0.0",
    "bip39": "^3.1.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.11"
  }
}
